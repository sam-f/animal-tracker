<% content_for :heading, "Edit #{@animal.name}" %>

<div class="field is-grouped">
  <%= link_to "Back", dashboard_animals_path, class: "button is-primary control" %>
  <%= button_to "Delete", {action: "destroy", id: @animal.id}, method: :delete, class: "button is-danger control" %>
</div>

<hr />

<%= render partial: "form" %>
